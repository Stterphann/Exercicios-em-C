#include<stdio.h>

int main () {

int x, y, soma;

    printf ("Digite o valor de X: ");
    scanf ("%d", &x);

    printf ("Digite o valor de Y: ");
    scanf ("%d", &y);

    soma = x + y;

    printf ("\nA soma dos valores corresponde a %d\n", soma);

return 0;
}
